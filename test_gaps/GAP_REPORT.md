<!--
  Generated by testgap_local.ps1
  Date:     2026-04-22 17:23
  Codebase: nmon -- a Python terminal system/GPU monitor. Collects GPU metrics via NVML and nvidia-smi, stores samples in SQLite, and renders a live TUI (Rich-based) with a dashboard, history view, and configurable widgets. Stack: Python 3.10+, rich, pynvml, readchar, TOML config, pytest test suite.
  Source:   src/phonebook (10 files)   Test: tests
  LLM:      qwen3-coder:30b
  Per-file gap reports: test_gaps/<src_path>.gap.md
-->
# Test Gap Report: nmon

## Coverage Summary

| Source File | Coverage | HIGH | MEDIUM | LOW |
|-------------|----------|------|--------|-----|
| src/nmon/__init__.py | NONE | 5 | 0 | 0 |
| src/nmon/config.py | PARTIAL | 3 | 2 | 0 |
| src/nmon/crud.py | PARTIAL | 4 | 1 | 0 |
| src/nmon/database.py | NONE | 4 | 0 | 0 |
| src/nmon/main.py | PARTIAL | 3 | 2 | 0 |
| src/nmon/models.py | NONE | 3 | 0 | 0 |
| src/nmon/routes/__init__.py | NONE | 2 | 0 | 0 |
| src/nmon/routes/computers.py | PARTIAL | 5 | 0 | 0 |
| src/nmon/routes/export.py | PARTIAL | 3 | 2 | 0 |
| src/nmon/schemas.py | NONE | 8 | 2 | 0 |

## High Priority Gaps

### `src/nmon/config.py` — Test validation of PORT field range (0-65535)
- **Gap:** No test for validation of PORT field range (0-65535) that could cause runtime crashes
- **Risk:** Invalid port numbers could cause runtime crashes or unexpected behavior
- **Write:** Add a test that passes an out-of-range port number to `get_settings()` and verifies `ValueError` is raised

### `src/nmon/config.py` — Test DATABASE_URL validation with pydantic
- **Gap:** No test for DATABASE_URL validation with pydantic that could allow malformed URLs to pass
- **Risk:** Malformed database URLs could cause connection failures at runtime
- **Write:** Add a test that passes an invalid database URL to `get_settings()` and verifies `ValueError` is raised

### `src/nmon/config.py` — Test empty .env file handling
- **Gap:** No test for empty .env file handling that could cause parsing errors
- **Risk:** Empty or whitespace-only .env files could cause parsing errors or unexpected behavior
- **Write:** Add a test that passes an empty .env file and verifies graceful handling with default fallback

### `src/nmon/crud.py` — Test `search_computers` with empty query parameter
- **Gap:** No test for `search_computers` raising `ValueError` when q is empty string
- **Risk:** Empty search queries could cause silent failures or unexpected behavior
- **Write:** Add a test that calls `search_computers` with empty query and verifies `ValueError` is raised

### `src/nmon/crud.py` — Test `update_computer` with non-existent computer_id
- **Gap:** No test for `update_computer` returning `None` when computer doesn't exist
- **Risk:** Update operations on non-existent records could cause unexpected behavior or crashes
- **Write:** Add a test that calls `update_computer` with non-existent ID and verifies `None` is returned

### `src/nmon/crud.py` — Test `delete_computer` with non-existent computer_id
- **Gap:** No test for `delete_computer` returning `False` when computer doesn't exist
- **Risk:** Delete operations on non-existent records could cause unexpected behavior or crashes
- **Write:** Add a test that calls `delete_computer` with non-existent ID and verifies `False` is returned

### `src/nmon/crud.py` — Test `get_computer` with non-existent computer_id
- **Gap:** No test for `get_computer` returning `None` when computer doesn't exist
- **Risk:** Retrieval of non-existent records could cause unexpected behavior or crashes
- **Write:** Add a test that calls `get_computer` with non-existent ID and verifies `None` is returned

### `src/nmon/main.py` — Test `create_app()` with invalid settings that would cause debug flag misconfiguration
- **Gap:** No test for `create_app()` with invalid settings that would cause debug flag misconfiguration
- **Risk:** Invalid settings could cause incorrect debug behavior or application startup issues
- **Write:** Add a test that passes invalid settings to `create_app()` and verifies debug flag behavior

### `src/nmon/main.py` — Test `lifespan()` exception handling when `init_db()` fails during startup
- **Gap:** No test for `lifespan()` exception handling when `init_db()` fails during startup
- **Risk:** Database initialization failures during startup could cause unhandled exceptions
- **Write:** Add a test that mocks `init_db()` to raise an exception and verifies proper error handling

### `src/nmon/routes/computers.py` — Test HTMX response behavior for all routes
- **Gap:** No test for HTMX response behavior for all routes (list, detail, edit, create, update, delete)
- **Risk:** Missing HTMX handling could cause UI rendering issues in production
- **Write:** Add tests for each route that verify proper HTMX response behavior with `is_hx_request` logic

## Broken or Misleading Tests

- `src/nmon/config.py::test_get_settings_raises_error_invalid_port` — Uses `SettingsError` which is not defined in the source code
- `src/nmon/config.py::test_get_settings_raises_error_malformed_database_url` — Uses `SettingsError` which is not defined in the source code
- `src/nmon/crud.py::test_computer_create_validation_friendly_name_empty` — Tests validation but doesn't verify it's raised by the CRUD function, not the Pydantic model
- `src/nmon/crud.py::test_computer_create_validation_friendly_name_too_long` — Tests validation but doesn't verify it's raised by the CRUD function, not the Pydantic model
- `src/nmon/crud.py::test_computer_create_validation_rustdesk_id_empty` — Tests validation but doesn't verify it's raised by the CRUD function, not the Pydantic model
- `src/nmon/crud.py::test_computer_create_validation_rustdesk_id_too_long` — Tests validation but doesn't verify it's raised by the CRUD function, not the Pydantic model
- `src/nmon/crud.py::test_computer_create_validation_invalid_local_ip` — Tests validation but doesn't verify it's raised by the CRUD function, not the Pydantic model
- `src/nmon/main.py::test_create_app_registers_routers` — Uses generic route count check instead of validating specific router registration
- `src/nmon/main.py::test_create_app_instantiates_jinja_templates` — Only checks for attribute existence, not proper template directory instantiation
- `src/nmon/main.py::test_app_has_lifespan_context_manager` — Only validates existence, not functionality
- `src/nmon/routes/computers.py::test_create_computer_duplicate_rustdesk_id` — Tests duplicate handling but doesn't verify the specific error message or form rendering behavior
- `src/nmon/routes/computers.py::test_edit_computer` — Tests update functionality but doesn't verify HTMX response behavior
- `src/nmon/routes/computers.py::test_search_route_returns_match` — Tests search functionality but doesn't verify the actual HTML content returned

## Mock and Fixture Quality Issues

- `src/nmon/main.py` > `override_get_db` — The fixture function doesn't actually override anything - it's a placeholder that does nothing
- `src/nmon/main.py` > `client` — Doesn't actually use the `override_get_db` fixture to patch dependencies
- `src/nmon/routes/computers.py` > `override_get_db` — Not used in any tests, but the fixture exists to override database dependency - this is good for test isolation

## Untested Files

### `src/nmon/__init__.py`
1. Test config validation and default value handling in `Settings`
2. Verify settings retrieval works with environment overrides in `get_settings`
3. Test database session creation and dependency injection in `get_db`
4. Verify database schema initialization with test engine in `init_db`
5. Test CRUD creation with valid/invalid data in `create_computer`

### `src/nmon/database.py`
1. Test database initialization with different database URLs (SQLite/PostgreSQL) and error handling for invalid URLs in `init_db`
2. Test session generation, proper yield/c
